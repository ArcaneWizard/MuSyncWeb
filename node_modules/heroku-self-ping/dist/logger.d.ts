declare namespace Logger {
    const setLogger: (newLogger: (arg: string) => any) => void;
    const setIsVerbose: (value: boolean) => void;
    const log: (arg: string) => void;
    const verbose: (arg: string) => void;
}
export default Logger;
